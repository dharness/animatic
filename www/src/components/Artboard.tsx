import styled from "styled-components";

const StyledArtboard = styled.div`
  background: green;
`;

function Artboard() {
    return (<StyledArtboard>Artboard</StyledArtboard>);
}

export default Artboard;